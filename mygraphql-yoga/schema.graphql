type Query {
  info: String!
  lights: [Light!]!
}

type Mutation {
  switch(id: ID!, on: Boolean!): Light
  toggle(id: ID!): Light
  dim(id: ID!, brightness: Int!): Light
}

""" type Mutation {
  post(url: String!, description: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}

type Counter {
  count: Int!
  countStr: String
}

type Subscription {
  newLink: Link
  counter: Counter!
} """

type Light {
  id: ID!
  name: String!
  description: String!
  isOn: Boolean!
  type: LightType!
  brightness: Int
}

enum LightType {
  DISCRETE
  DIMMABLE
  RGB
}