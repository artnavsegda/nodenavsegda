type Query {
  info: String!
  lights: [Light!]!
}

""" type Mutation {
  post(url: String!, description: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}

type Counter {
  count: Int!
  countStr: String
}

type Subscription {
  newLink: Link
  counter: Counter!
} """

type Light {
  id: ID!
  name: String!
  description: String!
  isOn: Boolean!
}